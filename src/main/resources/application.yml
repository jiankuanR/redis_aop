spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/store?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      idle-timeout: 60000
      maximum-pool-size: 30
      minimum-idle: 10



#showSql
logging:
  level:
    com:
      example:
        mapper: debug

spring.redis.host=39.106.159.126
spring.redis.port=6379